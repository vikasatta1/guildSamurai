import React from 'react';
import './Header.scss'

const Header = () => {
    return (
        <div className={'header'}>
            <h1>Blogger Platform</h1>
        </div>
    );
};

export default Header;